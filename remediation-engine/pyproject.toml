[project]
name = "remediation-engine"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "LetItGo90", email = "austinmundy90@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
    "pygithub>=2.9.1",
    "pyyaml>=6.0.3",
]

[project.scripts]
remediation-engine = "remediation_engine:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
